# Este es mi primer archivo .R del repositorio
# Vamos a crear una función que te devuelve la suma de dos números
# CAMBIOS REALIZADOS POR RAMANUEVA2!!!

suma <- function(a, b, c){
  d <- a + b + c
  e <- 1.25 * d
  return(e)
}
